import 'package:flutter/widgets.dart';

class TierListsOverviewPage extends StatelessWidget {
  const TierListsOverviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Tier Lists Overview Page')
    );
  }
}