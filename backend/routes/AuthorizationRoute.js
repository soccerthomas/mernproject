import express from 'express';
import jwtAuth from '../config/jwtAuth.js';

const router = express.Router();

//add login route

//add register route

//router.get('/example_URL', jwtAuth, (req, res) => {});